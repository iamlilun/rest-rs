// #[derive(Clone)]
// pub struct AppContainer {
//     pub db: DatabaseConnection,
//     pub user_repo: Arc<UserRepo>,
// }

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
